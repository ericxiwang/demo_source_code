apiVersion: v1
kind: Service
metadata:
  name: {{ include "react-frontend.fullname" . }}
spec:
  selector:  {{- include "react-frontend.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
