apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-backend.fullname" . }}
spec:
  selector:  {{- include "flask-backend.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
